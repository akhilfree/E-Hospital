import React from "react";
import { Component } from "react";

const Pass = (props)=>{
        return(
            <div className="gh">
                <h2>Haii {props.data}</h2>
            </div>
        );
        }

export default Pass;